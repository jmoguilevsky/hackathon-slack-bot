enum ConnectionLabelsTypes {
  basic = 'BasicAuthenticationConnection',
  oAuth = 'PlatformManagedOAuthConnection',
  personalToken = 'ApiKeyAuthenticationConnection',
}

export default ConnectionLabelsTypes;
